---
description: Supabase 数据库与向量检索约定
globs: "**/*.{ts,tsx}"
alwaysApply: false
---

# Supabase 约定

## 客户端初始化

- Supabase 客户端单例放在 `lib/supabase.ts`
- 服务端使用 `createServerClient`，客户端使用 `createBrowserClient`
- 环境变量：`NEXT_PUBLIC_SUPABASE_URL`、`NEXT_PUBLIC_SUPABASE_ANON_KEY`

## 向量检索（RAG）

- 向量嵌入存储使用 pgvector 扩展
- 相似度检索使用 RPC 函数 `match_documents`
- Embedding 模型调用封装在 `services/embedding.ts`

```typescript
const { data } = await supabase.rpc('match_documents', {
  query_embedding: embedding,
  match_threshold: 0.78,
  match_count: 5,
})
```

## 文献存储

- 文献元数据存在 `literature` 表
- 文献内容向量化后存在 `document_chunks` 表
- 文件存储使用 Supabase Storage
